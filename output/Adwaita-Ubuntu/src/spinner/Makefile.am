themedir = $(datadir)/icons/Adwaita-Ubuntu/src/spinner
theme_DATA =   
EXTRA_DIST = $(theme_DATA)
install-data-local: install-themeDATA
	(cd "$(DESTDIR)$(themedir)" && $(ICONMAP) -c spinner )
MAINTAINERCLEANFILES = Makefile.in
