themedir = $(datadir)/icons/Adwaita-Ubuntu/Adwaita/32x32
theme_DATA =   
EXTRA_DIST = $(theme_DATA)
install-data-local: install-themeDATA
	(cd "$(DESTDIR)$(themedir)" && $(ICONMAP) -c 32x32 )
MAINTAINERCLEANFILES = Makefile.in
